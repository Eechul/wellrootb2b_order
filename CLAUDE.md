# wellrootb2b_order

카페24 기반 **웰루트 B2B(도매)몰**의 **소량 발주 자동화 도구**.

이 몰은 **10개 미만 주문을 수동으로** 하게 되어 있다. 상품마다 들어가 담고 주문하는 반복을 없애는 게 목적이다.

**MVP:** 엑셀에 상품 URL·수량·주문자 정보를 적어 넣으면, 프로그램이 **브라우저를 열고 클릭해가며 주문**한다.

**스택:** Python 3.12 + Playwright. GUI 없이 CLI 우선. 대상 몰: `https://wellrootb2b.com`

> **현재 상태(2026-08-12): MVP 동작함.** 실제 발주 엑셀 + 2배송지 테스트로 로그인 → 담기 → 주문서 →
> 배송지 → 예치금 적용까지 끝까지 확인했다(결제 직전 정지). 남은 건 본품 옵션 상품 검증과 멱등성.
>
> **왜 주문을 하나로 못 묶나:** 주문마다 **수령인·배송지가 다르다**(고객 직배송). 장바구니는 한 주소로만 간다.
> → **주문 건수 = 배송지 개수**. 이건 업무의 본질이라 자동화로 못 줄인다.
> 대신 건당 손품(상품 찾기·수량·**주소 타이핑**·예치금)을 없앤다. 사람은 **결제 버튼만** 누른다.
> 같은 수령인이 2가지 이상 주문하면 장바구니로 묶어 한 주문으로 만든다.
>
> API 연동 경로는 조사 끝에 **보류**했다 — 이 몰은 예치금 결제인데 카페24는 예치금을 API로 차감할 수 없다.
> 브라우저 자동화는 실제 결제 화면을 거치므로 이 문제가 아예 생기지 않는다. 근거: [architecture-decisions](.claude/memory/architecture-decisions.md)

## 프로젝트 메모리

이 저장소의 누적 지식은 **`.claude/memory/`** 에 있다 (홈 글로벌 경로 아님).
작업 전에 관련 파일을 읽고, 새로 알게 된 비자명한 사실은 같은 형식으로 추가한 뒤 `MEMORY.md` 인덱스에 한 줄 넣는다.

| 파일 | 내용 |
|---|---|
| [site-facts](.claude/memory/site-facts.md) | **먼저 읽을 것** — wellrootb2b.com 실측 사실과 클릭 함정 |
| [mvp-browser-automation](.claude/memory/mvp-browser-automation.md) | MVP 형태, 설계 원칙, 예상 난관 |
| [stack-decision](.claude/memory/stack-decision.md) | 왜 Python+Playwright인가, 왜 결제 직전에 멈추는가 |
| [project-overview](.claude/memory/project-overview.md) | 무엇을 왜 만드는가, 목표와 범위 |
| [architecture-decisions](.claude/memory/architecture-decisions.md) | 왜 API가 아니라 브라우저 자동화인가 (경로 비교) |
| [cafe24-api-constraints](.claude/memory/cafe24-api-constraints.md) | 공식 문서 검증 기록 — 나중에 API로 확장할 때 다시 볼 것 |
| [open-questions](.claude/memory/open-questions.md) | 착수 전 확정할 사항 |
| [memory-location-preference](.claude/memory/memory-location-preference.md) | 메모리는 프로젝트 `.claude/memory/`에 기록 |

## 동작 흐름

```
엑셀 (상품URL · 옵션 · 수량 · 수취인/연락처/주소 · 배송메모)
   ▼  파싱 · 검증 (브라우저 띄우기 전에 최대한 걸러낸다)
   ▼  수취인+연락처+주소가 같은 라인끼리 묶기 → 배송지 그룹 = 주문 1건
로그인 (세션 재사용) + 보유 예치금 표시
   ▼
배송지 그룹마다 반복:
   ├ 이 배송지가 이미 결제된 이력이면 건너뜀 (--force로 무시)
   ├ 장바구니에 남은 게 있으면 비움
   ├ 그룹의 라인마다: 상품 이동 → 옵션 → 수량 → 담기   ← 성공/실패 개별 기록
   ├ 장바구니 전체상품주문 → 주문서
   ├ 배송지 입력 + 예치금 '사용' 클릭 → **잔액 부족이면 여기서 중단**
   ├ ⛔ 정지. 사람이 [결제하기] → Enter (q 입력 시 중단)
   └ 장바구니가 비었는지 확인 → 비었으면 이력에 '결제 확인' 기록
   ▼
라인별 결과 리포트 + 준비/결제확인 건수
```

**결제 완료 여부는 장바구니로 판정한다.** 카페24는 주문이 완료되면 장바구니를 비우므로,
결제 후에도 항목이 남아 있으면 결제되지 않은 것이다. 이력에도 남기지 않는다.

**예치금 부족 판정도 결제금액으로 한다.** 예치금 '사용' 후 남은 결제금액이 0보다 크면
그만큼 모자란 것이다. 이 몰은 예치금으로만 결제되므로 결제 자체가 불가능하니 거기서 멈춘다.
(적용 후 결제금액이 0으로 표시되는 건 정상 — 몰 안내 문구에도 그렇게 적혀 있다.)

## 구조

```
app.py             ★ GUI (Tkinter). 사장님들이 실제로 쓰는 진입점
order.py           CLI 진입점 — 개발·디버깅용
runner.py          ★ 발주 흐름 본체. GUI와 CLI가 이 하나를 공유한다
                     화면 출력(log)과 결제 대기(ask_payment)만 콜백으로 빠져 있다
excel_reader.py    엑셀 파싱·검증·배송지별 그룹핑. 사이트 DOM과 무관 = 안 깨짐
mall_selectors.py  ★ 셀렉터 전부 여기에. 다른 파일에 셀렉터를 쓰지 말 것
                     ⚠ selectors.py로 되돌리지 말 것 — 파이썬 표준 모듈을 가린다
browser.py         설치된 Chrome → Edge → 내장 크로미움 순으로 실행
credentials.py     설정 읽기/쓰기. 비밀번호는 윈도우 DPAPI로 암호화
updater.py         자동 업데이트 (update.json 확인 → 내려받기 → 교체 후 재시작)
version.py         VERSION(배포할 때마다 올린다) + 문의 오픈채팅 주소
dialog.py          alert/confirm 자동 수락 + 메시지 보관(실패 사유가 여기 들어온다)
history.py         실행 이력 — 중복 발주 차단 + 끊긴 발주 이어하기
steps/
  login.py         세션 재사용 로그인, 보유 예치금 읽기
  cart.py          담기 (라인별 실패 격리) + 장바구니 비우기 + 전체상품주문
  order_form.py    배송지 입력, 예치금 '사용' — ⛔ 결제 버튼 없음
report.py          라인별 성공/실패, 부분 성공 경고
build.ps1          배포용 exe 빌드 + update.json 생성
storage.py         ★ 설정·이력·세션을 어디에 둘지 한 곳에서 정한다
```

## 실행

```powershell
pip install -r requirements.txt

python app.py                            # GUI (기본)

python order.py --template               # 발주 엑셀 템플릿 생성
python order.py 발주.xlsx --dry-run      # 엑셀 검증 + 배송지 그룹 확인 (브라우저 안 띄움)
python order.py 발주.xlsx                # CLI 실행
python order.py 발주.xlsx --clear-cart   # 기존 장바구니를 비우고 시작
python order.py 발주.xlsx --force        # 이미 결제된 배송지도 다시 주문
```

**브라우저는 설치된 Chrome/Edge를 쓴다.** `playwright install`이 필요 없고, exe에 브라우저를 넣지 않는다.
개발 PC에 받아둔 크로미움은 마지막 폴백일 뿐이다.

## 설정·이력이 저장되는 곳

`storage.py`가 정한다. **exe 옆이 아니다.**

```
%LOCALAPPDATA%\WellrootOrder\
├─ config.json      몰 주소·아이디·암호화된 비밀번호
├─ session.json     로그인 쿠키
└─ history.jsonl    결제 확인된 배송지 (수령인·주소 = 개인정보)
```

**왜 exe 옆이 아닌가:** 사장님이 exe를 바탕화면에 두면 파일이 흩뿌려진다.
지저분해서 지우면 계정이 날아가고, **이력을 지우면 중복 발주 방지가 풀린다.**
개인정보가 눈에 띄는 곳에 있는 것도 문제고, `Program Files`에 두면 쓰기 권한이 없어 아예 저장이 안 된다.

**exe 옆에 `config.json`이 있으면 그 폴더를 쓴다(휴대용 모드).** USB로 들고 다니는 경우와
예전 방식 사용자를 위한 것이라 마이그레이션이 필요 없다. `--selftest`가 어느 모드인지 알려준다.
[설정] 창의 **[설정 폴더 열기]** 로 사장님이 직접 찾아갈 수 있다.

## 배포 (거래처 사장님들께)

저장소: https://github.com/Eechul/wellrootb2b_order · 배포는 **GitHub Releases**

```powershell
git push                                    # ★ 코드를 먼저 올린다 (안 하면 태그가 어긋난다)
$env:WELLROOT_NOTES = "이번 변경 내용"
.\build.ps1                                 # dist\ 에 exe + update.json
.\dist\'웰루트 발주도우미.exe' --selftest    # ★ 배포 전 필수 — 결과는 selftest.log
.\release.ps1                               # GitHub 릴리스로 업로드
```

`release.ps1`은 커밋 안 된 변경이나 미push 상태면 **스스로 거부한다.**
버전·체크섬이 어긋나도 막는다.

**실측(2026-08-12): 48.3MB / GUI 기동 2.6초 / 메모리 53MB.**

`--selftest`는 창을 띄우지 않고 브라우저 실행·페이지 로드·암호화까지 실제로 해본다.
`--windowed`라 콘솔이 없어 그냥 실행하면 실패가 안 보이므로 **배포 전에 반드시 돌린다.**

- Playwright의 Node 드라이버(node.exe 86MB)는 반드시 들어간다. 브라우저는 안 들어간다.
- `build.ps1`의 `--exclude-module` 목록을 지우지 말 것 — 없으면 numpy 등이 딸려와 68MB가 된다.
- **onefile은 부모/자식 두 프로세스로 뜬다.** 부모만 죽이면 자식이 exe를 잠가 다음 빌드가 실패한다.
- **onefile에서 `__file__`은 임시 압축해제 폴더다.** 설정·이력·세션은 반드시 `sys.executable` 옆에 둔다
  (`app.py`의 `app_dir()`). 안 그러면 설정이 매 실행마다 사라진다.
- **exe 이름은 ASCII로 유지할 것.** 한글 파일명은 업데이트 배치와 다운로드 URL에서 문제를 만든다.
- **Program Files에 두지 말 것.** 자동 업데이트가 자기 자신을 교체하므로 사용자가 쓰기 가능한 위치여야 한다.
- 자동 업데이트는 `config.json`의 `update_url`이 가리키는 `update.json`을 본다.
  버전이 올라가 있으면 내려받아 sha256을 검증하고, 앱 종료를 기다렸다 교체 후 재실행한다.
- 비밀번호는 **윈도우 DPAPI**로 사용자 계정에 묶어 암호화한다. 설정 파일을 복사해 가도 다른 PC에서는 안 풀린다.

**중간에 끊겼으면 같은 엑셀을 그냥 다시 돌리면 된다.** 결제가 확인된 배송지는 이력을 보고
자동으로 건너뛰므로, 이어하기와 중복 방지가 같은 동작이다.

스킨이 바뀌어 셀렉터가 깨지면 `playwright codegen https://wellrootb2b.com` 으로
실제 화면을 클릭해 셀렉터를 다시 뽑는다. 막히면 항상 codegen부터.

## 엑셀 컬럼

컬럼 **순서는 상관없다**(이름으로 찾는다). 예전 양식의 `연락처`는 휴대폰번호로 인식한다.

| 컬럼 | 주문서 칸 | 필수 |
|---|---|---|
| 상품url | — | ✅ |
| 수량 | — | ✅ |
| 수취인 | 수취자 성명 | ✅ |
| **전화번호** | 일반전화 `#rphone1_*` | ✅ (몰이 `isFill`로 요구) |
| 우편번호 | 우편번호 | ✅ |
| 주소 | 기본주소 | ✅ |
| 휴대폰번호 | 휴대전화 `#rphone2_*` | 선택 |
| 상세주소 | 나머지 주소 | 선택 |
| 배송메모 | 배송 메세지 | 선택 |
| 옵션 | 상품 옵션 | 선택 (`(없음)` 표기 가능) |

**🚨 선택 컬럼이 빈칸이면 주문서의 해당 칸도 비운다.** 건너뛰면 안 된다 —
주문서에는 기본배송지·최근 배송메모가 미리 채워져 있어서, 그대로 두면
**앞사람의 연락처·상세주소·메모가 다른 수령인에게 그대로 간다.** 실측 사례는 [site-facts](.claude/memory/site-facts.md).

휴대폰번호 칸에 지역번호(`02-`, `031-`)를 적으면 **엑셀 검증에서 막는다** —
주문서의 휴대전화 앞자리 select에 지역번호가 아예 없어서 넣을 수가 없고, 두 칸을 바꿔 적은 실수일 가능성이 높다.

## 사용자에게 보이는 문구

**사장님들은 개발자가 아니다.** 사용자 눈에 닿는 문구에 다음을 노출하지 말 것:

- 파일 이름(`config.json`) → **"[설정]에서"**
- CSS 셀렉터(`#rphone2_2`) → **칸 이름**("휴대폰번호 칸")
- CLI 옵션(`--clear-cart`, `--force`) → **행동 안내**("장바구니를 비우신 뒤 다시 실행")
- raw 예외(`TimeoutError: Locator.click...`) → `runner.py`의 `_friendly_error()`가 번역한다.
  우리가 만든 `RuntimeError` 안내문은 그대로 통과시키고, 예상 못 한 것만 번역한다.

문구는 **무엇을 하면 되는지**로 끝나야 한다. 원인 문자열은 담당자 전달용으로 괄호 안에 한 줄만.

창 오른쪽 아래 **[문의하기 · 자동화 문의]** 가 카카오톡 오픈채팅을 연다(`version.py`의 `SUPPORT_URL`).
사장님이 막혔을 때 갈 곳을 만들어주는 게 목적이다 — 오류 문구에서 "담당자에게 알려주세요"라고 하면
여기를 누르면 된다. 주소를 바꾸면 배포된 앱에도 반영되니 링크가 살아 있는지 확인하고 올릴 것.

로그인 실패는 `steps/login.py`의 `_login_failure_reason()`이 **화면을 읽어 원인을 특정**한다
(캡차 / 휴면·잠금 / 아이디·비밀번호 불일치 / 접속 실패). 무조건 "비밀번호 확인"이라고 하면
캡차일 때 엉뚱한 곳을 헤매게 된다.

## 그림 (아이콘 · 하단 배너)

`assets/` 폴더에 넣으면 자동으로 쓰인다. **없어도 앱은 정상 동작한다.**

| 파일 | 쓰이는 곳 | 규격 | 상태 |
|---|---|---|---|
| `assets/app.ico` | exe 아이콘 + 창 제목 왼쪽 | ICO, 정사각형, **16~256px 7종 다중 포함** | ✅ 있음 |
| `assets/footer.png` | 창 맨 아래 | PNG, **가로 696px 고정**, 세로 40~80px | 없음(선택) |

아이콘은 로고 전체가 아니라 **나무+화살표 심볼**만 쓴다 — 16px에서 글자는 얼룩이 된다.
로고가 바뀌면 `assets/logo_source.png`를 갈아끼우고 `python assets\make_icon.py`를 돌린다
(Pillow 필요). `SYMBOL_BOX`가 잘라낼 영역이고, 실행하면 작은 크기 미리보기가 같이 나온다.

**Tkinter는 그림 크기를 조절하지 못한다** — 넣은 크기 그대로 그려지므로 규격을 맞춰 받아야 한다.
경로는 `resources.asset()`이 찾는다. onefile에서 동봉 파일은 `sys._MEIPASS`에 풀리므로
**설정 파일 위치(exe 옆)와 다르다** — 헷갈리면 개발 중엔 되는데 exe에서만 그림이 안 뜬다.

## 설계 원칙 (타협하지 말 것)

- **🚨 지금은 결제 버튼을 누르지 않는다.** 예치금이 잘못 빠지면 주문 취소 처리를 해야 해서 되돌리는 비용이 크다.
  **결제 자동화는 확정된 방향이지만**(2026-08-12), 넘기기 전에 갖춰야 할 것들이 있다
  — 주문번호 확보, 금액 상한, 이상치 검증. 순서와 기준은 [auto-payment-roadmap](.claude/memory/auto-payment-roadmap.md).
  `mall_selectors.py`에 `#btn_payment`를 넣기 전에 그 문서를 먼저 읽을 것.
- **셀렉터는 한 파일에 모은다.** 카페24 스킨은 몰마다·업데이트마다 DOM이 다르다.
  흩어져 있으면 스킨이 바뀔 때마다 전수 수색이다. 깨졌을 때 **빨리 고칠 수 있는 구조**가 최우선.
- **부분 성공을 조용히 넘기지 않는다.** 일부 라인이 실패했는데 나머지로 주문이 나가면
  "일부만 주문된 상태"가 된다. 라인별 결과를 반드시 리포트하고, 실패가 있으면 사용자에게 명확히 알린다.
- **🚨 시작 전 장바구니가 비어 있어야 한다.** 기존 항목이 남아 있으면 전체상품주문에 **함께 딸려 들어간다.**
  비어 있지 않으면 멈추고, `--clear-cart`를 줘야만 비운다. 이 안전장치를 없애지 말 것.
- **멱등성.** 같은 엑셀을 두 번 돌려 중복 주문이 나가는 게 이 도메인 최악의 사고다.
  `history.jsonl`에 **결제가 확인된** 배송지만 기록하고, 재실행 시 그 배송지는 건너뛴다.
  준비만 하고 결제 안 된 건은 기록하지 않는다 — 안 한 주문을 했다고 표시하는 게 더 위험하다.
- **본품 옵션 상품은 MVP 범위 밖이다(2026-08-12 사용자 결정).** 지금 다루는 건 추가상품 옵션뿐.
  본품 필수 옵션 상품을 만나면 담기가 실패하고 몰의 alert 메시지가 사유로 리포트된다.
- **예치금 잔액을 우리 쪽에서 계산하거나 흉내 내지 말 것.** 차감은 카페24가 한다.
- **거래처별 단가는 없다(전부 동일).** 금액 계산 로직을 새로 만들지 말 것 — 몰 가격을 그대로 쓴다.

## 주의사항

- **계정 정보를 코드·저장소에 넣지 말 것.** 별도 설정 파일 또는 OS 자격증명 저장소.
- 운영 몰에서 테스트하면 **실제 주문·예치금**에 영향이 간다. 결제 직전에서 멈추는 원칙이 곧 테스트 안전장치다.
- 로그인 실패를 반복하면 자동입력 방지문자(캡차)가 뜰 수 있다. 세션(쿠키)을 저장해 로그인 횟수를 줄인다.
- 한글 인코딩: Windows 터미널에서 UTF-8 설정 필요. 엑셀 경로에 한글·공백이 섞인다는 전제로 처리.
